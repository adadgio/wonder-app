# Uncomment the next line to define a global platform for your project
target 'wonder-app' do
  platform :ios, '9.0'
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'AppCenter'

  # Pods for wonder-app

end
